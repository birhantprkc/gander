# Rendered frames, stills, contact sheets and the film itself. Regenerable and large, the
# same reason listing-video and store-art ignore theirs. The generator here is the source.
out/

# Jost is fetched, not committed, the way scripts/make-wordmark.py fetches it. It is SIL OFL
# and could legally live here; it stays out because this repo has never carried a font file
# and a clone can get it back with the two curl lines in README.md.
fonts/

# The narrator's model (325 MB, Apache-2.0, fetched: see README.md) and the venv that runs it.
models/
.venv/
